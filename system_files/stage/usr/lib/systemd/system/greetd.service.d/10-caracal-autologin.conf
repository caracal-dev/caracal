[Unit]
# Ensure the autologin config is regenerated (uid-1000 user resolved) before
# greetd reads /etc/greetd/config.toml.
Wants=caracal-stage-autologin.service
After=caracal-stage-autologin.service
